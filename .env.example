# ── Production ────────────────────────────────────────────
# Anthropic API key (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Wallet address to receive USDC payments
PAY_TO_ADDRESS=0x...

# Network: "base" for production, "base-sepolia" for testing
NETWORK=base

# x402 facilitator (default: https://x402.org/facilitator)
FACILITATOR_URL=https://x402.org/facilitator

# ── E2E testing (optional) ───────────────────────────────
# Client wallet (the "buyer" paying for API access)
TEST_PRIVATE_KEY=0x...
TEST_WALLET_ADDRESS=0x...

# Gateway wallet (the "seller" — same as PAY_TO_ADDRESS for tests)
TEST_GATEWAY_ADDRESS=0x...

# Tollbooth gateway URL
TEST_GATEWAY_URL=http://localhost:3000
